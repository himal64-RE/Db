-- 1. Create Employee Table

CREATE TABLE employee (
    EmpID NUMBER PRIMARY KEY,
    FirstName VARCHAR2(20),
    LastName VARCHAR2(20),
    Gender CHAR(1),
    DateOfBirth DATE,
    Designation VARCHAR2(50),
    DepartmentName VARCHAR2(50),
    ManagerID NUMBER,
    JoinedDate DATE,
    EmpSalary NUMBER(10,0),
    EmpAddress VARCHAR2(200)
);


-- 2. Insert Employee Records

INSERT INTO employee VALUES(
1,
'Himal',
'Adhikari',
'M',
TO_DATE('2007-06-17','YYYY-MM-DD'),
'Engineer',
'Software Engineering',
5,
TO_DATE('2022-04-01','YYYY-MM-DD'),
500000,
'Sanothimi-Bhaktapur'
);

INSERT INTO employee VALUES(
2,
'Ram',
'Thapa',
'M',
TO_DATE('2009-06-17','YYYY-MM-DD'),
'Engineer',
'Software Engineering',
7,
TO_DATE('2022-04-05','YYYY-MM-DD'),
5250000,
'Lokanthali-Bhaktapur'
);

INSERT INTO employee VALUES(
3,
'Sita',
'Shrestha',
'F',
TO_DATE('2003-06-18','YYYY-MM-DD'),
'Manager',
'Software Engineering',
8,
TO_DATE('2021-05-12','YYYY-MM-DD'),
600000,
'Sanepa-Lalitpur'
);


-- 3. Update the gender of employee whose ID = 2

UPDATE employee
SET Gender = 'F'
WHERE EmpID = 2;


-- 4. Display all employees

SELECT * FROM employee;


-- 5. Display First Name, Current Date, DOB and Age of employees older than 30

SELECT
FirstName,
SYSDATE AS CurrentDate,
DateOfBirth,
FLOOR(MONTHS_BETWEEN(SYSDATE, DateOfBirth)/12) AS Age
FROM employee
WHERE FLOOR(MONTHS_BETWEEN(SYSDATE, DateOfBirth)/12) > 30;


-- 6. Find the oldest employee

SELECT *
FROM employee
WHERE DateOfBirth = (
    SELECT MIN(DateOfBirth)
    FROM employee
);


-- 7. Find the youngest employee

SELECT *
FROM employee
WHERE DateOfBirth = (
    SELECT MAX(DateOfBirth)
    FROM employee
);


-- 8. Display maximum salary department-wise

SELECT
DepartmentName,
MAX(EmpSalary) AS MaxEmpSalary
FROM employee
GROUP BY DepartmentName;


-- 9. List employees who act as managers

SELECT FirstName
FROM employee
WHERE EmpID IN (
    SELECT ManagerID
    FROM employee
);


-- 10. Display the most recently joined employee

SELECT *
FROM employee
WHERE JoinedDate = (
    SELECT MAX(JoinedDate)
    FROM employee
);


-- 11. Pattern matching queries

SELECT * FROM employee
WHERE FirstName LIKE 'R%';

SELECT * FROM employee
WHERE LastName LIKE '%a';

SELECT * FROM employee
WHERE FirstName LIKE 'S%n';

SELECT * FROM employee
WHERE EmpAddress LIKE 'L%';